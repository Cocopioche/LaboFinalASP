@model IEnumerable<ChatManager.Models.User>

@{
    ViewBag.Title = "Friendships";
}

<h2>Friendships</h2>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FirstName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            
          
            @Helper.UserRow(item.User, item.GetRelation(item.User) , false)
              
            <div>

            </div>

        </td>
        <td>
           
        </td>
    </tr>
}

</table>
