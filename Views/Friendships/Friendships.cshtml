@model IEnumerable<ChatManager.Models.FriendshipsView>

@{
    ViewBag.Title = "Friendships";
}

<h2>Friendships</h2>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.User.FirstName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            
            @Helper.AvatarUser(item.User)
              
            <div>

            </div>

        </td>
        <td>
           
        </td>
    </tr>
}

</table>
